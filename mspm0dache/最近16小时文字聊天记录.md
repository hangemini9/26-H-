# 最近16小时文字聊天记录

> 导出时间：2026-07-28。仅保留可见的文字对话；图片、截图和附件二进制内容未嵌入。早期部分的助手答复若当前上下文未保留逐字原文，会标注为“按当时结论整理”，不会冒充逐字记录。

## 项目继承与底盘调试

**用户：** 这个项目里好像少了一个对话，是这样吗？

**用户：** `C:\Users\Han\Desktop\dIANSAI\diansai(1)\mspm0dache` 阅读这个项目的 `AGENT.MD` 以及简要浏览，你现在继承这个项目的对话。

**用户：** 好的，接下来继续调试底盘，该调试什么了？

**用户：** 以后每完成一次代码的更新或者知识的变动，都要在 `AGENT.MD` 里面更新日志，方便以后智能体之间工作的交接，明白了吗？

**助手：** 已接受项目继承与交接规则；后续每次代码或知识变化均同步更新 `AGENT.MD`。

## TI 板外部供电与编码器

**用户：** 多次询问：TI 板用电池经稳压模块单独供电，是否接 J11 区域的 `3V3` 和 `GND`？

**助手（按当时结论整理）：** J11 是外部 3.3 V/GND 目标供电点；稳压输出必须先调好并实测为 3.3 V。外部供电时需要按板卡跳帽规则隔离 XDS110 供电，不能把电池或 5 V 直接接入 J11 3V3。

**用户：** 接了 J11 的 3V3/GND 灯不亮，如图。

**助手（按当时结论整理）：** 板上未亮的元件不一定是目标板电源 LED；应直接测 J11 两端电压和目标板实际供电状态。

**用户：** 再从编码器调起吧，还是先告诉我编码器怎么调。

**用户：** 转动车轮后数据不变化。

**用户：** 编码器要接 5V 电压，更正一下。

**用户：** 我编码器供电直接从 MSPM0 的 5V 供电，测得供电接口有 5V。

**用户：** PB0/PB16 是 0.96V 不变，PB6/PB7 是 4.14V 不变；不论挪动车轮还是停住，电压表持续 4.44V。

**助手（按当时结论整理）：** 编码器可由稳定 5 V 供电，但 A/B 原始输出如为 5 V，必须经电平转换后再接 MSPM0 普通 GPIO；不得将约 4.14 V 直接送入 3.3 V MCU IO。

**用户：** 现在编码器好了，只不过车前进编码器是负的，后退是正的。

**助手（按当时结论整理）：** 已修改/确认编码器方向，使车辆前进计数为正；随后编码器计数恢复正常。

## 轮速 PID 与底盘测试

**用户：** 现在直接调 PID 吧。把需要的数据打包成一个 `g_test_pid` 结构体，便于在 WATCH 里添加截图。

**用户：** 我按你的操作来，大概一秒多轮子转起来就停了。

**用户：** 我设置的目标 RPM 是 80；MODE7 车轮后退且编码器计数不动；不要这么多调试变量，只留一个 `G_TEST` 和必要变量。

**助手（按当时结论整理）：** 调试接口随后整理为唯一 `g_test`；排查编码器监督/方向问题后继续进行闭环轮速调试。

**用户：** 编码器 OK 了。右轮稍微慢一点，而且感觉速度起来得很快。刚测了 100 转，在相等时间下两个轮转的路程几乎相等，是不是可以跳过 PID？

**助手（按当时结论整理）：** 记录了已可用的 PID 参数：Kp=0.7、Ki=20、Kd=0；空转和短距离结果满足继续后续模块调试的条件，但实际循迹仍沿用闭环 PID 与编码器保护。

**用户：** 空转没问题，但是下地测试电机抖、会发出响声。

**用户：** 轮子的事情解决了，就是有东西卡着导致的响动。

## JDY-31 蓝牙

**用户：** 我这里有一个蓝牙模块，电脑也有蓝牙，能用蓝牙模块通过电脑调试吗？可以用蓝牙启动调试模式吗？

**用户：** 这个问题解决了，现在弄蓝牙模块；它是 HC-05 好像，如图。

**助手（按当时结论整理）：** 根据照片识别为 JDY-31，而非 HC-05；UART0 接线为 MSPM0 PA10/PA11，电脑通过 SPP 串口连接。

**用户：** 我现在显示已配对，但是没连接，这个对吗？

**用户：** OK 了，是 COM7；运行脚本后收到了 `PONG 2026072805`、状态和 `OK STOP`。

**助手（按当时结论整理）：** 已确认 COM7 为可用的 Windows SPP 端口；“已配对未连接”在未打开串口时可以是正常现象，打开 COM7 脚本时才建立实际串口连接。

## 巡线模块接入、标定与通道确认

**用户：** 我现在要给小车加上循迹模块，使用幻尔 8 路巡线传感器。

**用户：** 赛道是黑线白底；模块安装在车头正前方；车头朝前时 S1 在左边；SDA/SCL 两个空闲电压都是稳定 3.31V。

**助手（按当时结论整理）：** 选择 I2C1：SCL→PB2/BP9，SDA→PB3/BP10；模块使用稳定 5 V 和公共地。先实现只读传感器遥测，不立刻控制电机。

**用户：** 我刚不小心动了哪里，代码编译没通过，你改一下。

**助手（按当时结论整理）：** 清理重建与完整验证成功，未发现需要保留的源代码损坏。

**用户：** 截图中 `connected=0`、`sample_sequence=0`、`error_count=369`。

**助手（按当时结论整理）：** 判断为 I2C 每次读取失败；增加传感器上电等待与 I2C 错误恢复，升级到 build 2026072807。

**用户：** 连上了，接下来该干什么了？截图中 `connected=1`、采样序号增长、`error_count=0`。

**助手：** 通信已经正常；要求在最终安装高度完成模块标定，并采集白底、中心黑线、S1、S8 的 `state/analog/threshold`。

**用户：** 应该是标定完了。（附中心黑线照片与模拟量/阈值截图）

**助手：** 从照片判断黑线约经过 S5/S6；继续要求用白底、S1 和 S8 单独验证数字位序和极性。

**用户：** 图一全白底 `STATE=0`；图二黑线经过最左侧 S1，`STATE=1`；图三黑线经过最右侧 S8，`STATE=128`。

**助手：** 确认 bit0=S1（车辆左）、bit7=S8（车辆右），置位代表黑线；加入 `active_count`、`line_lost`、`position_x1000` 观测，build 2026072808。

**用户：** 数据正确，加入低速循迹吧。

## 当前循迹与蓝牙控制

**助手：** 实现 build 2026072809：

- 新增 mode 8 低速黑线循迹；默认基速 30 RPM、转向 Kp 6。
- 黑线偏右时左轮加速、右轮减速；偏左时相反。
- 保留 PID、编码器监督、40% PWM 限幅、到时停车与 STOP。
- I2C 断连、采样停滞或连续丢线超过 100 ms：以 `status=-6` 停车解除使能。

**用户：** 我在这其中不会连接调试线，而是使用蓝牙发布调试命令。

**助手：** 蓝牙协议增加：`ARM` 后 `LINE` 启动 5 秒 mode 8 验证；后续 `LAP` 启动 90 秒 mode 8 跑圈。脚本增加 `-Line` 与 `-Lap`，每秒自动请求状态并持续发送 `KEEP`。

**用户：** 顺带一提，我有一个黑线为矩形的测试场地，20 秒不够，跑完一圈至少需要一分钟。

**助手：** 保留 `LINE` 为 5 秒架空/短时验证；增加 `LAP` 为 90 秒完整跑圈。90 秒内仍受 KEEP 心跳、STOP、传感器丢线、编码器和输出故障保护；mode 8 绝对上限为 120 秒。

**助手：** build 2026072809 已完成 TEST 与 Release 离线验证；尚未烧录进行实际循迹运动测试。

## 最近蓝牙链路检查说明

**用户：** 在这之前要先检查蓝牙是否连接，告诉我如何检查。

**助手：** 仅测试链路、不运动：

```powershell
powershell -ExecutionPolicy Bypass -File "C:\Users\Han\Desktop\dIANSAI\diansai(1)\mspm0dache\tools\bluetooth_control.ps1" -Port COM7
```

预期收到：

```text
PONG 2026072809
S M=0 ST=0 T=0 L=0 R=0 P=... B=... X=...
Link test only...
```

收到 `PONG 2026072809` 即证明电脑、COM7、蓝牙模块和 TI 板串口链路正常；不带 `-Line`、`-Lap` 或 `-Run` 时不会启动电机。

## 当前下一步

**助手：** 烧录 build 2026072809 后：

1. 先执行不带运动参数的 COM7 链路测试；
2. 架空驱动轮并保持巡线模块标定高度，执行 `-Line`；
3. 验证居中、偏左、偏右和丢线停车；
4. 通过后进行 5 秒下地 `-Line`；
5. 最后在矩形赛道执行 `-Lap`（90 秒）。
