# Agent entry point

Read [AGENT.MD](AGENT.MD) completely before modifying this project.

`AGENT.MD` is the canonical hardware/software handoff. Keep its verified
state, assumptions, safety invariants, build instructions, and next actions
synchronized with every material firmware or wiring change.
